Test ensures that dependencies and devDependencies are picked up.

Transitive dependencies in project have licenses using SPDX identifiers, licenses refered to by name, and extractable license text files.
